# Dependencies
node_modules/
**/node_modules/

# Runtime data / databases / volumes (NEVER commit — can be GBs)
**/data/
*.sqlite
*.sqlite3
*.db

# Build output
dist/
build/
.next/
.vite/

# Secrets / env
.env
.env.*
!.env.example
**/keys/
*.private.key
# Filled Kubernetes Secret (copy of secret.example.yaml with real values) — never commit
**/infra/k8s/secret.yaml

# Logs / OS
*.log
.DS_Store
Thumbs.db

# Rust build artifacts
target/
**/target/

# Large rrweb replay served by the web (do not commit ~26MB)
apps/web/public/*.bmmreplay

*.bmmreplay
