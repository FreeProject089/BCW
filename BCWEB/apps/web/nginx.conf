server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;
  # SPA: client-side routing falls back to index.html. (Caddy handles /api + /telemetry.)
  location / { try_files $uri $uri/ /index.html; }
}
